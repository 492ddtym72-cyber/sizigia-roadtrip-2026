function fmtDate(iso){return iso?new Date(iso+'T12:00:00').toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'}):'';}
function fmtMoney(v){return String(v||'').replace(/^(\d+(?:[.,]\d+)?)\s*€/,'€$1');}

export function draftBody(state,search,candidate,place,mode){
  const name=place?.name||candidate.name||'Camping',dates=search.startDate&&search.endDate?`from ${fmtDate(search.startDate)} to ${fmtDate(search.endDate)}`:`for the night of ${search.dateLabel||''}`,party=`${(state.crew||[]).length} adults travelling with one camper and one small car`,end='Thank you very much.\n\nKind regards,\n\n';
  if(mode==='reserve'){const priceKnown=candidate.finalPrice?`the expected total is ${fmtMoney(candidate.finalPrice)}${candidate.price&&candidate.tax?` (${fmtMoney(candidate.price)} plus tourist tax)`:''}`:candidate.price?`the quoted price is ${fmtMoney(candidate.price)}${candidate.tax?', plus tourist tax':''}`:candidate.tax?'tourist tax is charged separately':'',parkingKnown=candidate.parking?(/außerhalb|outside/i.test(candidate.parking)?'the small car can be parked in the parking area outside the campsite':'the parking arrangement for the small car is clear from your reply'):'',known=[priceKnown,parkingKnown].filter(Boolean),missing=[!(candidate.finalPrice||candidate.price)&&'the total price',!candidate.parking&&'where the small car can be parked'].filter(Boolean),questions=[missing.length&&`Could you please also confirm ${missing.join(' and ')}?`,'Please let us know when the reservation becomes definitive and whether any further step or payment is required.'].filter(Boolean).join(' ');return `Dear ${name} team,\n\nThank you for confirming that you can offer us a place ${dates}.\n\nWe would like to accept your offer and reserve it for ${party}.${known.length?`\n\nWe understand that ${known.join(' and ')}.`:''}\n\n${questions}\n\n${end}`;}
  if(mode==='call'){const walkIn=/vorbeifahren|spontaneous arrival|walk.?in/i.test((candidate.nextAction||'')+' '+(candidate.reply||'')),arrival=search.startDate?fmtDate(search.startDate):search.dateLabel||'the arrival day';return walkIn?`Dear ${name} team,\n\nThank you for confirming that we may arrive spontaneously ${dates}. We understand that a pitch cannot be guaranteed in advance for ${party}.\n\nWe will contact you shortly before arrival to check the current situation.\n\n${end}`:`Dear ${name} team,\n\nThank you for confirming that we may call you on ${arrival} to ask about last-minute availability ${dates} for ${party}.\n\nWe will contact you ${candidate.callWindow?'during your reception hours ('+candidate.callWindow+')':'on the day before travelling to you'}.\n\n${end}`;}
  if(mode==='dimensions'){const v=(state.vehicles||[]).find(x=>x.id==='v-camper')||{};if(!v.lengthM)throw new Error('Camperlänge fehlt');return `Dear ${name} team,\n\nThank you for your reply. The camper is ${v.lengthM} metres long${v.widthM?`, ${v.widthM} metres wide`:''}${v.heightM?` and ${v.heightM} metres high`:''}${v.model?` (${v.model})`:''}.\n\nCould you please check whether a suitable place is available ${dates}?\n\n${end}`;}
  if(mode==='missing')return `Dear ${name} team,\n\nThank you for your reply regarding our stay ${dates}. Could you please also confirm the total price and whether our small car can be parked at or near the place?\n\n${end}`;
  if(mode==='deposit')return `Dear ${name} team,\n\nThank you for the booking information for our stay ${dates}. Before we arrange the deposit, could you please confirm the required amount, payment deadline and reference we should include with the payment?\n\n${end}`;
  if(mode==='followup')return `Dear ${name} team,\n\nI am following up regarding availability ${dates} for ${party}. Has a pitch become available in the meantime?\n\nIf you are still full, would it be possible to call you spontaneously on the day in case of a cancellation?\n\n${end}`;
  if(mode==='clarify')return `Dear ${name} team,\n\nThank you for your reply. Before we proceed, could you please confirm that the offer applies ${dates} to ${party}, including parking for the small car?\n\n${end}`;
  return `Dear ${name} team,\n\nI would like to ask if you have availability ${dates} for ${party}.\n\nIf advance reservations are not available or you are currently full, would it still be possible to call you spontaneously on the day in case a pitch becomes available?\n\nCould you please also let me know the total price and whether the small car can be parked at or near the pitch?\n\n${end}`;
}

export function draftSubject(search,candidate,mode){
  if(candidate.mailThreadSubject)return candidate.mailThreadSubject.replace(/^(?:re|r|aw):\s*/i,'Re: ');
  return mode==='reserve'?`Reservation request – ${search.dateLabel||''} – ${candidate.name}`:mode==='followup'?`Follow-up: Pitch availability ${search.dateLabel||''}`:`Pitch availability ${search.dateLabel||''} – ${candidate.name}`;
}
