#!/usr/bin/env node
// Deterministische Datenbrücke für den Camping-Mail-Assistenten.
// Apple Mail wird ausschließlich von der kontrollierten Automation gelesen;
// diese Brücke erzeugt Vorlagen und schreibt konfliktgeschützt nach Firebase.
import fs from 'node:fs';
import {draftBody as sharedDraftBody,draftSubject as sharedDraftSubject} from './camping-mail-templates.mjs';

const ROOT=new URL('../',import.meta.url);
const appSource=fs.readFileSync(new URL('app.js',ROOT),'utf8');
const CLOUD_URL=appSource.match(/CLOUD_URL\s*=\s*'([^']+)'/)?.[1]
  || fs.readFileSync(new URL('index.html',ROOT),'utf8').match(/CLOUD_URL\s*=\s*'([^']+)'/)?.[1];
if(!CLOUD_URL)throw new Error('CLOUD_URL nicht gefunden');
const command=process.argv[2]||'scan';
async function getState(){const r=await fetch(CLOUD_URL,{headers:{'X-Firebase-ETag':'true'},cache:'no-store'});if(!r.ok)throw new Error('Firebase GET '+r.status);return {state:await r.json(),etag:r.headers.get('etag')};}
function nextStamp(prev){const now=Date.now(),old=Date.parse(prev||'')||0;return new Date(Math.max(now,old+1)).toISOString();}
function nextRunStamp(){const now=new Date();for(const h of [8,14,20]){const d=new Date(now);d.setHours(h,0,0,0);if(d>now)return d.toISOString();}const d=new Date(now);d.setDate(d.getDate()+1);d.setHours(8,0,0,0);return d.toISOString();}
async function conditionalUpdate(mutator){
  for(let attempt=0;attempt<3;attempt++){
    const {state,etag}=await getState();const changed=await mutator(state);if(!changed)return state;
    state.meta=state.meta||{};state.meta.lastSaved=nextStamp(state.meta.lastSaved);
    const r=await fetch(CLOUD_URL,{method:'PUT',headers:{'Content-Type':'application/json','if-match':etag},body:JSON.stringify(state)});
    if(r.status===412)continue;if(!r.ok)throw new Error('Firebase PUT '+r.status);return state;
  }
  throw new Error('Firebase-Konflikt nach 3 Versuchen');
}
function appConstArray(name){const start=appSource.indexOf(`const ${name} =`),open=appSource.indexOf('[',start),close=appSource.indexOf('\n];',open);if(start<0||open<0||close<0)throw new Error(`${name} nicht gefunden`);return Function(`"use strict";return (${appSource.slice(open,close+2)})`)();}
function appConstSet(name){const start=appSource.indexOf(`const ${name} =`),open=appSource.indexOf('new Set(',start),close=appSource.indexOf(');',open);if(start<0||open<0||close<0)throw new Error(`${name} nicht gefunden`);return Function(`"use strict";return (${appSource.slice(open,close+1)})`)();}
function stableSlug(value){return String(value||'').normalize('NFKD').replace(/[^a-zA-Z0-9]+/g,'-').replace(/^-|-$/g,'').toLowerCase().slice(0,64);}
function networkCounts(state){const searches=(state.sleepSearches||[]).filter(s=>s.mode==='network');return {hubs:searches.length,candidates:searches.reduce((n,s)=>n+(s.candidates||[]).length,0),places:(state.sleepPlaces||[]).length};}
async function seedNetwork(){
  const hubs=appConstArray('CAMPING_NETWORK_HUBS'),seeds=appConstArray('CAMPING_NETWORK_CANDIDATES'),firstNightSeeds=appConstArray('CAMPING_FIRST_NIGHT_ADDITIONS_V13'),verified=appConstSet('CAMPING_NETWORK_VERIFIED'),snapshot=await getState(),backupUrl=new URL('../backups/firebase-pre-v13-route-leads-2026-07-14.json',import.meta.url),seasideBackupUrl=new URL('../backups/firebase-pre-v18-seaside-2026-07-17.json',import.meta.url);
  if(!fs.existsSync(backupUrl))fs.writeFileSync(backupUrl,JSON.stringify(snapshot.state,null,2)+'\n',{mode:0o600});
  if(!snapshot.state?.meta?.seasideCampingOptions&&!fs.existsSync(seasideBackupUrl))fs.writeFileSync(seasideBackupUrl,JSON.stringify(snapshot.state,null,2)+'\n',{mode:0o600});
  const before=networkCounts(snapshot.state),result=await conditionalUpdate(state=>{
    state.sleepSearches=Array.isArray(state.sleepSearches)?state.sleepSearches:[];state.sleepPlaces=Array.isArray(state.sleepPlaces)?state.sleepPlaces:[];let changed=false,added=0;
    for(const hub of hubs){let search=state.sleepSearches.find(s=>s.networkKey===hub.id);if(!search){search={id:`net-v12-${hub.id}`,title:hub.title,startDate:hub.startDate,endDate:hub.endDate,arrivalWindowStart:hub.arrivalWindowStart,arrivalWindowEnd:hub.arrivalWindowEnd,dateLabel:`${hub.startDate.split('-').reverse().join('.')}–${hub.endDate.split('-').reverse().join('.')}`,region:hub.region,maxDrive:'flexibler Korridor',mode:'network',networkKey:hub.id,corridorOrder:hub.order,coverageTarget:hub.target,createdAt:new Date().toISOString(),candidates:[]};state.sleepSearches.push(search);changed=true;}search.candidates=Array.isArray(search.candidates)?search.candidates:[];for(const key of ['title','region','startDate','endDate','arrivalWindowStart','arrivalWindowEnd'])if(search[key]!==hub[key]){search[key]=hub[key];changed=true;}const dateLabel=`${hub.startDate.split('-').reverse().join('.')}–${hub.endDate.split('-').reverse().join('.')}`;if(search.dateLabel!==dateLabel){search.dateLabel=dateLabel;changed=true;}if(search.corridorOrder!==hub.order){search.corridorOrder=hub.order;changed=true;}if(search.coverageTarget!==hub.target){search.coverageTarget=hub.target;changed=true;}}
    const norm=value=>String(value||'').trim().toLocaleLowerCase('de');
    for(const seed of seeds){const search=state.sleepSearches.find(s=>s.networkKey===seed.hub);if(!search)continue;const isVerified=verified.has(seed.name);let place=state.sleepPlaces.find(p=>norm(p.name)===norm(seed.name)||(seed.email&&norm(p.email)===norm(seed.email)));if(!place){place={id:`place-v12-${stableSlug(seed.name)}`,createdAt:new Date().toISOString(),contactVerified:isVerified};state.sleepPlaces.push(place);changed=true;}for(const key of ['name','region','email','phone','link','officialUrl','contactFormUrl','lat','lng','notes'])if((place[key]===undefined||place[key]==='')&&seed[key]!==undefined){place[key]=seed[key];changed=true;}if(isVerified&&place.contactVerified!==true){place.contactVerified=true;changed=true;}let candidate=search.candidates.find(c=>c.placeId===place.id||norm(c.name)===norm(seed.name));if(!candidate){candidate={id:`candidate-v12-${seed.hub}-${stableSlug(seed.name)}`,placeId:place.id,name:seed.name,region:seed.region,email:seed.email,phone:seed.phone,link:seed.link,officialUrl:seed.officialUrl,contactFormUrl:seed.contactFormUrl||'',lat:seed.lat,lng:seed.lng,status:'new',preferred:!!seed.preferred,requestedArrivalDate:seed.requestedArrivalDate||'',requestedDepartureDate:seed.requestedDepartureDate||'',inquiryQuestion:seed.inquiryQuestion||'',notes:seed.notes||'',contactVerified:isVerified,nextAction:seed.contactFormUrl?'Kontaktformular für die konkrete Nacht vorbereiten':isVerified?'Ein-Nacht- und Verfügbarkeitsanfrage vorbereiten':'Offizielle Kontaktdaten vor Versand prüfen',contactedAt:null,repliedAt:null,draftState:'none',confirmation:{dates:false,party:false,camper:false,car:false}};search.candidates.push(candidate);changed=true;added++;}else if(candidate.status==='new'){if(candidate.preferred!==!!seed.preferred){candidate.preferred=!!seed.preferred;changed=true;}for(const key of ['requestedArrivalDate','requestedDepartureDate','inquiryQuestion'])if(!candidate[key]&&seed[key]){candidate[key]=seed[key];changed=true;}if(!candidate.notes&&seed.notes){candidate.notes=seed.notes;changed=true;}if(isVerified&&candidate.contactVerified!==true){candidate.contactVerified=true;changed=true;}if(isVerified&&candidate.nextAction==='Offizielle Kontaktdaten vor Versand prüfen'){candidate.nextAction=seed.contactFormUrl?'Kontaktformular für die konkrete Nacht vorbereiten':'Ein-Nacht- und Verfügbarkeitsanfrage vorbereiten';changed=true;}}
    }
    const firstNight=state.sleepSearches.find(s=>/erste nacht/i.test(s.title||''));
    if(firstNight){firstNight.candidates=Array.isArray(firstNight.candidates)?firstNight.candidates:[];for(const seed of firstNightSeeds){const isVerified=verified.has(seed.name);let place=state.sleepPlaces.find(p=>norm(p.name)===norm(seed.name)||(seed.email&&norm(p.email)===norm(seed.email)));if(!place){place={id:`place-v13-${stableSlug(seed.name)}`,createdAt:new Date().toISOString(),contactVerified:isVerified};state.sleepPlaces.push(place);changed=true;}for(const key of ['name','region','email','phone','link','officialUrl','lat','lng','notes'])if((place[key]===undefined||place[key]==='')&&seed[key]!==undefined){place[key]=seed[key];changed=true;}if(isVerified&&place.contactVerified!==true){place.contactVerified=true;changed=true;}let candidate=firstNight.candidates.find(c=>c.placeId===place.id||norm(c.name)===norm(seed.name));if(!candidate){candidate={id:`candidate-v13-first-night-${stableSlug(seed.name)}`,placeId:place.id,...seed,status:'new',preferred:false,contactVerified:isVerified,nextAction:'Ein-Nacht- und Verfügbarkeitsanfrage vorbereiten',contactedAt:null,repliedAt:null,draftState:'none',confirmation:{dates:false,party:false,camper:false,car:false}};firstNight.candidates.push(candidate);changed=true;added++;}}}
    state.meta=state.meta||{};if(state.meta.frenchCritAirResearch!=='2026-07-14'){state.meta.frenchCritAirResearch='2026-07-14';changed=true;}if(state.meta.frenchPreferredContactsVerified!=='2026-07-14'){state.meta.frenchPreferredContactsVerified='2026-07-14';changed=true;}if(state.meta.routeLeadResearch!=='2026-07-14'){state.meta.routeLeadResearch='2026-07-14';changed=true;}if(state.meta.seasideCampingOptions!=='2026-07-17'){state.meta.seasideCampingOptions='2026-07-17';changed=true;}if((Number(state.schemaVersion)||0)<18){state.schemaVersion=18;changed=true;}if(changed){state.log=Array.isArray(state.log)?state.log:[];state.log.push({id:`network-v18-${Date.now()}`,ts:new Date().toISOString(),who:'Codex',desc:added?`hat ${added} geprüfte Küsten-Campingoptionen entlang der Route ergänzt`:'hat die Küstenoptionen geprüft',undo:null});state.log=state.log.slice(-150);}return changed;
  });
  return {ok:true,before,after:networkCounts(result),backup:backupUrl.pathname};
}
function locate(state,searchId,candidateId){const search=(state.sleepSearches||[]).find(x=>x.id===searchId),candidate=search?.candidates?.find(x=>x.id===candidateId),place=(state.sleepPlaces||[]).find(x=>x.id===candidate?.placeId);return {search,candidate,place};}
async function scan(){
  const {state}=await getState();
  const candidates=[];(state.sleepSearches||[]).forEach(search=>(search.candidates||[]).forEach(c=>{const p=(state.sleepPlaces||[]).find(x=>x.id===c.placeId)||{};candidates.push({searchId:search.id,candidateId:c.id,name:p.name||c.name,region:p.region||c.region,email:p.email||c.email,phone:p.phone||c.phone,link:p.link||c.link,lat:p.lat??c.lat,lng:p.lng??c.lng,status:c.status,dateLabel:search.dateLabel});}));
  return {runnerMode:state.mailAssistant?.runnerMode||'local',processedMessageIds:state.mailAssistant?.processedMessageIds||[],candidates,draftRequests:(state.mailAssistant?.draftRequests||[]).filter(x=>['opened','requested','ready','fallback'].includes(x.status)),reviewQueue:(state.mailAssistant?.reviewQueue||[]).filter(x=>x.status==='pending')};
}
async function draftPayload(requestId){const {state}=await getState(),req=(state.mailAssistant?.draftRequests||[]).find(x=>x.id===requestId);if(!req)throw new Error('Entwurfsanfrage nicht gefunden');const {search,candidate,place}=locate(state,req.searchId,req.candidateId);if(!search||!candidate)throw new Error('Campingplatz zur Entwurfsanfrage nicht gefunden');const view={...candidate,name:place?.name||candidate.name};return {requestId:req.id,searchId:req.searchId,candidateId:req.candidateId,messageId:req.messageId||'',to:place?.email||candidate.email||'',subject:sharedDraftSubject(search,view,req.template),body:sharedDraftBody(state,search,candidate,place,req.template),template:req.template};}
async function setMode(value){if(!['local','shadow','cloud'].includes(value))throw new Error('Modus muss local, shadow oder cloud sein');return conditionalUpdate(state=>{state.mailAssistant=state.mailAssistant||{};state.mailAssistant.runnerMode=value;return true;});}
async function applyEvents(encoded){
  const events=JSON.parse(Buffer.from(encoded,'base64url').toString('utf8'));
  return conditionalUpdate(state=>{
    const a=state.mailAssistant||(state.mailAssistant={}),now=new Date().toISOString();
    a.processedMessageIds=Array.isArray(a.processedMessageIds)?a.processedMessageIds:[];a.draftRequests=Array.isArray(a.draftRequests)?a.draftRequests:[];a.reviewQueue=Array.isArray(a.reviewQueue)?a.reviewQueue:[];a.runners=a.runners||{};const local=a.runners.local||(a.runners.local={});let changed=events.length===0,hadError=false;
    if(Date.parse(a.lease?.expiresAt||'')>Date.now())throw new Error('Cloud-Mail-Check läuft; lokales Anwenden wurde sicher abgebrochen');
    for(const e of events){
      if(e.assistantError!==undefined){a.lastError=local.lastError=String(e.assistantError||'');hadError=!!local.lastError;changed=true;continue;}
      if(e.draftRequest){if(!a.draftRequests.some(x=>x.id===e.draftRequest.id))a.draftRequests.push(e.draftRequest);changed=true;continue;}
      if(e.reviewItem){if(!a.reviewQueue.some(x=>x.id===e.reviewItem.id))a.reviewQueue.push({...e.reviewItem,excerpt:String(e.reviewItem.excerpt||'').slice(0,600)});changed=true;continue;}
      if(e.messageId&&a.processedMessageIds.includes(e.messageId))continue;
      const {candidate,place}=locate(state,e.searchId,e.candidateId);if(!candidate)continue;
      ['status','reply','replyQuote','nextAction','nextActionDate','price','tax','finalPrice','deposit','bookingRef','cancellationDeadline','arrivalWindow','offeredArrivalDate','offeredDepartureDate','parking','pitchNote','callWindow','mailMessageId','mailThreadSubject','contactedAt','repliedAt','draftState','confirmedAt'].forEach(k=>{if(e[k]!==undefined)candidate[k]=e[k];});
      ['region','email','phone','link','lat','lng'].forEach(k=>{if(e[k]===undefined)return;const target=place||candidate;if(place?.contactVerified===true)return;target[k]=e[k];if(['email','phone','link'].includes(k))target.contactVerified=false;});
      if(e.requestId){const req=a.draftRequests.find(x=>x.id===e.requestId);if(req){if(e.requestStatus)req.status=e.requestStatus;if(e.readyAt)req.readyAt=e.readyAt;if(e.sentAt)req.sentAt=e.sentAt;if(e.error)req.error=e.error;}}
      if(e.messageId&&!a.processedMessageIds.includes(e.messageId))a.processedMessageIds.push(e.messageId);changed=true;
      if(e.status||e.reply){state.log=Array.isArray(state.log)?state.log:[];state.log.push({id:`mail-${Date.now()}-${Math.random().toString(36).slice(2,8)}`,ts:now,who:'Mail-Assistent',desc:`hat die Antwort von „${place?.name||candidate.name}“ ausgewertet`,undo:null});state.log=state.log.slice(-60);}
    }
    a.processedMessageIds=a.processedMessageIds.slice(-500);a.reviewQueue=a.reviewQueue.slice(-50);a.lastRunAt=local.lastRunAt=now;a.nextRunAt=local.nextRunAt=nextRunStamp();if(!hadError){a.lastSuccessAt=local.lastSuccessAt=now;a.lastError=local.lastError='';}return changed;
  });
}

try{
  if(command==='self-test'){const sample={crew:Array(6),vehicles:[{id:'v-camper',lengthM:'6.4'}]},search={startDate:'2026-08-02',endDate:'2026-08-03',arrivalWindowStart:'2026-08-02',arrivalWindowEnd:'2026-08-03',region:'Liguria',mode:'network'},candidate={name:'Test Camping'},place={name:'Test Camping'},modes=['inquiry','network_policy','followup','reserve','call','dimensions','missing','deposit','clarify'],bodies=modes.map(mode=>sharedDraftBody(sample,search,candidate,place,mode)),hubs=appConstArray('CAMPING_NETWORK_HUBS'),network=appConstArray('CAMPING_NETWORK_CANDIDATES'),firstNight=appConstArray('CAMPING_FIRST_NIGHT_ADDITIONS_V13'),verified=appConstSet('CAMPING_NETWORK_VERIFIED'),preferred=network.filter(x=>x.preferred),exact=sharedDraftBody(sample,search,{name:'Exact',requestedArrivalDate:'2026-08-03',requestedDepartureDate:'2026-08-04'},place,'inquiry');if(bodies.some(body=>body.includes('Frederic')||body.includes('roadtrip group')||!body.endsWith('Kind regards,\n\n')))throw new Error('Signatur-Test fehlgeschlagen');if(bodies[3].includes('I would like to ask if you have availability'))throw new Error('Reservierungsantwort wiederholt Verfügbarkeitsanfrage');if(!bodies[1].includes('route remains flexible')||!bodies[1].includes('one-night stay'))throw new Error('Routen-Vorlage fehlt');if(!bodies[0].includes('arriving on any day from')||!bodies[0].includes('only need one night'))throw new Error('Flexibles Anreisefenster fehlt');if(!exact.includes('from 3 August 2026 to 4 August 2026')||exact.includes('choose any available arrival date'))throw new Error('Platzspezifische Wunschnacht fehlt');if(hubs.length!==8||network.length!==47||firstNight.length!==1||preferred.length!==11||preferred.some(x=>!verified.has(x.name))||firstNight.some(x=>!verified.has(x.name)))throw new Error('Camping-Netz V18 unvollständig');console.log(JSON.stringify({ok:true,templates:modes.length,blankSignature:true,reservationDoesNotRepeatInquiry:true,networkPolicy:true,flexibleWindow:true,exactRequestedStay:true,hubs:hubs.length,candidates:network.length,firstNight:firstNight.length,preferred:preferred.length,preferredVerified:true}));}
  else if(command==='draft-payload'){console.log(JSON.stringify(await draftPayload(process.argv[3]||'')));}
  else if(command==='set-mode'){await setMode(process.argv[3]||'');console.log(JSON.stringify({ok:true,runnerMode:process.argv[3]}));}
  else if(command==='seed-network'){console.log(JSON.stringify(await seedNetwork(),null,2));}
  else if(command==='apply'){await applyEvents(process.argv[3]||'');console.log(JSON.stringify({ok:true}));}
  else {console.log(JSON.stringify(await scan(),null,2));}
}catch(e){console.error(JSON.stringify({ok:false,error:String(e.message||e)}));process.exitCode=1;}
